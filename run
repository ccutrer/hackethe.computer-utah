#!/usr/bin/env ruby

#require 'byebug'

file = $stdin
file = File.open(ARGV[0]) if ARGV[0]

cubes = file.read.split("\n\n").map { |c| c.split("\n") }
biggest = cubes.map(&:length).max

puts biggest
sleep 5 if biggest > 3
